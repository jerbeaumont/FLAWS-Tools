if(NOT FLAWS_TOOLS_USE_FILE_INCLUDED)

   set(FLAWS_TOOLS_USE_FILE_INCLUDED 1)

   set(CMAKE_MODULE_PATH ${CMAKE_MODULE_PATH} ${FLAWS_TOOLS_CMAKE_DIRS})

   include_directories(${FLAWS_TOOLS_INCLUDE_DIRS})
   link_directories(${FLAWS_TOOLS_LIBRARY_DIRS})
   include(FLAWS_TOOLSExports)
   include(ITKLibs)

   set(BUILD_TOOLS ${BUILD_TOOLS})
   set(BUILD_MODULE_FLAWS-SEQUENCE ${BUILD_MODULE_FLAWS-SEQUENCE})

   set(ITK_DIR ${ITK_DIR})
   find_package(ITK REQUIRED)
   include(${ITK_USE_FILE})

   set(TCLAP_INCLUDE_DIR ${TCLAP_INCLUDE_DIR})
   include_directories(${TCLAP_INCLUDE_DIR})
   
endif(NOT FLAWS_TOOLS_USE_FILE_INCLUDED)
